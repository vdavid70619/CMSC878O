function g = logreg_grad(x,D,c)
    Nd = size(x,1);
    g = zeros(Nd,1);
    z = c.*(D*x);
    
    g(z>=0) = (c*D)'*(-(exp(-z)/(1+exp(-z)));
    g(z<0) = (c*D)'*(-1+exp(z(z<0))/(1+exp(z(z<0)));
    
    y = sum(f);

end